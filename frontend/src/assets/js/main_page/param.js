import main_method from "./main_page_method"

const main_param = {
    instrument: {
        len: 12,
        panel: 16,
        img: 'drum_'
    },
    progress: {
        len: 16
    },
    select: {
        inst: {
            list: ['jazz', 'rock', 'hiphop', 'percussion']
        }
    }
}

export default main_param